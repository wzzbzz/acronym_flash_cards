{% extends 'base.html.twig' %}

{% block body %}
<div class="container d-flex flex-column justify-content-center align-items-center vh-100 text-center">
    <h1 class="mb-5">Acronym Flashcards</h1>

    <a href="{{ path('flashcards', { mode: 'study' }) }}" class="btn btn-primary btn-lg w-100 mb-3">
        📚 Study Cards
    </a>
    {# <a href="{{ path('flashcards', { mode: 'test' }) }}" class="btn btn-outline-primary btn-lg w-100 mb-3">
        🧪 Test Mode
    </a> #}
    <a href="{{ path('acronym_directory_index') }}" class="btn btn-secondary btn-lg w-100">
        🗂️ View All Terms
    </a>
</div>
{% endblock %}
